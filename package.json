{
  "name": "agent-skills",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo of agent skills and the self-contained artifacts that power them",
  "type": "module",
  "engines": { "node": ">=22", "pnpm": ">=10" },
  "packageManager": "pnpm@11.5.3",
  "scripts": {
    "build": "pnpm -r --filter \"./artifacts/*\" run build",
    "dev": "pnpm --filter \"./artifacts/$0\" run dev",
    "typecheck": "pnpm -r run typecheck",
    "validate": "node scripts/validate-skills.mjs",
    "package": "node scripts/package-skill.mjs",
    "new-skill": "node scripts/new-skill.mjs",
    "check": "pnpm validate && pnpm typecheck && pnpm build && git diff --exit-code -- 'skills/*/assets'"
  }
}
